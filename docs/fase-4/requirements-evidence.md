# Phase 4 Requirement-to-Evidence Matrix

Source plan reviewed: `/var/www/jogo/plano-fase4-tech-challenge.html` lines 389-405 and final operational checklist lines 408-420.

## Executive status

The Phase 4 implementation has strong **code-boundary evidence** across the repositories: OS, Billing, and Execution services exist with Dockerfiles, REST contracts, persistence boundaries, RabbitMQ adapters/envelopes, saga orchestration logic, compensation paths, OpenAPI docs, tests, builds, and coverage reports above 80% in the local evidence files.

The remaining risk is mostly **operational proof** outside RabbitMQ, Kubernetes, and CI. RabbitMQ now has a local live-broker smoke harness covering OS, Billing, and Execution messaging code with shared `correlationId`s. Kubernetes now has local `kind` evidence proving render, server dry-run, live apply, `kubectl get pods/services`, RabbitMQ readiness, and OS `/health` + `/ready`; the evidence honestly documents that the shared platform Order/Billing/Execution placeholder images still block a full three-service Kubernetes E2E. CI/branch-protection evidence now proves green PR Validation and protected `stag`/`prod` branches for app, edge, platform, db, Billing, and Execution. Final PDF/video artifacts are still missing. Where tests are broker-free or manifests are only validated/rendered, this matrix labels them as code-boundary evidence rather than live operational evidence.

Status summary for the 17 rows below: **Complete: 9**, **Partial: 8**, **Missing: 0**.

## Requirement matrix

| Requirement | Evidence found | Commands/files/links | Status | Gap / next action |
|---|---|---|---|---|
| 3 microsserviços independentes | OS Service (`workshop-app`), Billing (`workshop-billing`), and Execution (`workshop-execution`) have separate repos, runtime code, health/ready endpoints, Dockerfiles, and independent validation evidence. Remote GitHub Actions now prove green PR Validation/`ci` for OS/app plus Billing and Execution. Platform manifests define three independent Deployments with placeholder images. | `workshop-app/Dockerfile`; `workshop-app/.github/workflows/pr-validation.yml`; app green PR Validation `https://github.com/tech-challenges-fiap/workshop-app/actions/runs/28380465166`; `workshop-app/docs/evidence/fase-4/f4-os-service-boundary.md`; `workshop-billing/Dockerfile`; Billing green PR Validation `https://github.com/tech-challenges-fiap/workshop-billing/actions/runs/29713968100`; `workshop-billing/docs/evidence/fase-4/f4-billing-service-scaffold.md`; `workshop-execution/Dockerfile`; Execution green PR Validation `https://github.com/tech-challenges-fiap/workshop-execution/actions/runs/29713969857`; `workshop-execution/docs/evidence/fase-4/f4-execution-service-scaffold.md`; `workshop-platform/kubernetes/base/services/{order-service,billing-service,execution-service}/deployment.yaml`; `workshop-platform/docs/evidence/fase-4/f4-platform-service-deployments.md`. | Partial | Replace platform placeholder images before full-service Kubernetes E2E. |
| Banco próprio por serviço | OS uses its own PostgreSQL schema and tests against local PostgreSQL. Billing has PostgreSQL configuration, migrations, repository layer, and readiness behavior. Execution has MongoDB config/repository/readiness. DB boundary docs prohibit shared central database and cross-service SQL access. | OS: `workshop-app/docs/evidence/fase-4/f4-os-service-boundary.md`, `f4-os-saga-orchestrator.md`, `src/infrastructure/db/schema/*`. Billing: `workshop-billing/docs/evidence/fase-4/f4-billing-postgres.md`, `src/database/migrations/*`. Execution: `workshop-execution/docs/evidence/fase-4/f4-execution-mongodb.md`. Legacy boundary: `workshop-db/docs/evidence/fase-4/f4-db-legacy-boundary.md`. | Partial | Operational proof is incomplete: Billing explicitly says no live PostgreSQL integration test was run; Execution defaults Mongo readiness off and does not prove live Mongo. Run/show service-specific DB connections or migrations in the final smoke/demo. |
| SQL + NoSQL | SQL evidence exists for OS PostgreSQL and Billing PostgreSQL; NoSQL evidence exists for Execution MongoDB. Coverage reports show persistence-boundary tests. | OS full suite in `workshop-app/docs/evidence/fase-4/f4-os-saga-orchestrator.md` (`418 pass`) and `f4-os-rabbitmq-events.md` (`426 pass`). Billing PostgreSQL evidence: `workshop-billing/docs/evidence/fase-4/f4-billing-postgres.md`. Execution MongoDB evidence: `workshop-execution/docs/evidence/fase-4/f4-execution-mongodb.md`. | Partial | Add an operational demo/query proving PostgreSQL and Mongo persistence with real local containers or deployed services, not only mocked repository tests. |
| REST + mensageria | REST/OpenAPI contracts are present for OS, Billing, and Execution. RabbitMQ publisher/consumer adapters, envelopes, event handlers, and config are implemented for OS/Billing/Execution. A local live RabbitMQ smoke harness proves OS → Billing → OS → Execution → OS happy path and failure/compensation path with shared `correlationId`s. Platform includes RabbitMQ manifests. Caveat: the smoke is a dedicated local harness around real messaging code, not a Kubernetes or three-HTTP-service-process E2E. | OpenAPI: `workshop-app/docs/openapi.yaml`; `workshop-billing/docs/openapi.yaml`; `workshop-execution/openapi/openapi.yaml`. Messaging: `workshop-app/docs/fase-4/rabbitmq-smoke-evidence.md`; `workshop-app/docs/fase-4/rabbitmq-full-smoke.ts`; `workshop-app/docs/evidence/fase-4/f4-os-rabbitmq-events.md`; `workshop-billing/docs/evidence/fase-4/f4-billing-rabbitmq-compensation.md`; `workshop-execution/docs/evidence/fase-4/f4-execution-rabbitmq-events.md`; platform broker: `workshop-platform/docs/evidence/fase-4/f4-platform-rabbitmq.md`. | Complete | For final demo, retain caveat that Kubernetes/service-process deployment is covered separately under Kubernetes evidence. |
| Saga Pattern | OS Service has durable saga state, deterministic transitions, saga tables, repository, orchestration use case, idempotency, and tests. | `workshop-app/docs/evidence/fase-4/f4-os-saga-orchestrator.md`; command evidence includes `bun test ...` targeted saga tests (`12 pass, 0 fail`) and full test suite (`418 pass, 0 fail`). | Complete | For final delivery, still include an operational query by `workOrderId` or `correlationId` after smoke, but the code-boundary requirement is evidenced. |
| Compensação | OS distributed-flow tests cover Billing failure compensation, Execution failure compensation, duplicate inbound events, and correlation propagation. Billing implements compensation handlers and outbound status/result events. Evidence is broker-free, not a live compensation demo. | `workshop-app/docs/evidence/fase-4/f4-os-distributed-flow.md`; targeted command `bun test src/application/work-order/work-order-distributed-flow.test.ts ...` (`12 pass, 0 fail`). `workshop-billing/docs/evidence/fase-4/f4-billing-rabbitmq-compensation.md`. | Partial | Run failure/compensation smoke through RabbitMQ and capture `RefundRequested` or terminal `COMPENSATED` state plus Billing refund/compensation record. |
| BDD/e2e | OS has distributed-flow tests for happy path and failure paths using in-memory publisher/repository boundaries. This is useful code-boundary evidence, but it is not a real cross-service e2e with broker and real services. No Gherkin/BDD suite was found in the inspected evidence. | `workshop-app/docs/evidence/fase-4/f4-os-distributed-flow.md`; command `bun test src/application/work-order/work-order-distributed-flow.test.ts src/application/work-order/orchestrate-work-order-saga.test.ts src/application/work-order/handle-inbound-work-order-saga-event.test.ts`. | Partial | Add or run a real e2e/smoke scenario against OS + Billing + Execution + RabbitMQ. If BDD is required by evaluator, add a small scenario file or clearly map the smoke script to the BDD happy path. |
| Cobertura 80% | Local coverage evidence exceeds 80% in Billing and Execution. OS has full test pass evidence but the inspected Phase 4 evidence did not include an explicit percentage for OS coverage. | Billing: `workshop-billing/docs/evidence/fase-4/f4-billing-payment-contracts.md` (`93.13% lines`), `f4-billing-rabbitmq-compensation.md` (`93.51% lines`). Execution: `workshop-execution/docs/evidence/fase-4/f4-execution-mongodb.md` (`90.20% lines`), `f4-execution-rabbitmq-events.md` (`91.72% lines`). OS script exists: `workshop-app/package.json` has `test:coverage`. | Partial | Run `bun run test:coverage` in `workshop-app` and archive output. Collect coverage artifacts or screenshots per service for PDF. |
| CI/CD independente | Remote green PR Validation evidence and local workflows prove independent CI for `workshop-app`, `workshop-edge`, `workshop-platform`, `workshop-db`, `workshop-billing`, and `workshop-execution`. App validates lint/test/build/Docker/k8s; Edge validates Terraform and Lambda lint/test/build/package; Platform validates Terraform and Kubernetes manifests; DB validates Terraform; Billing and Execution now each have a successful remote `PR Validation` workflow run with job `ci` on `stag`. Recent `stag` Deploy runs failed in app/edge/platform/db, although green PR Validation links exist. | `workshop-app/docs/fase-4/ci-branch-protection-evidence.md`; green PR Validation runs: app `https://github.com/tech-challenges-fiap/workshop-app/actions/runs/28380465166`, edge `https://github.com/tech-challenges-fiap/workshop-edge/actions/runs/28380471671`, platform `https://github.com/tech-challenges-fiap/workshop-platform/actions/runs/28380477901`, db `https://github.com/tech-challenges-fiap/workshop-db/actions/runs/28380485369`, Billing `https://github.com/tech-challenges-fiap/workshop-billing/actions/runs/29713968100` job `ci`, Execution `https://github.com/tech-challenges-fiap/workshop-execution/actions/runs/29713969857` job `ci`. | Complete | If deploy success is required by the evaluator rather than PR validation success, investigate or caveat failed latest `stag` deploys in app/edge/platform/db. |
| Kubernetes | Platform has RabbitMQ manifests and three service Deployment/Service/ConfigMap baselines; OS has its own k8s base/overlays. Validation scripts pass. New local `kind` evidence proves Kustomize render, server-side dry-run, live apply, `kubectl get pods/services`, RabbitMQ `1/1 Running` plus `rabbitmq-diagnostics ping`, and OS/app `1/1 Running` with `/health` and `/ready` HTTP 200. Caveat: shared platform Order/Billing/Execution Deployments still use `example.invalid/...:replace-me` placeholder images and therefore show `ImagePullBackOff`; this is not a full three-service Kubernetes E2E. | `workshop-app/docs/fase-4/kubernetes-evidence.md`; helper: `workshop-app/docs/fase-4/kubernetes-kind-postgres-helper.yaml`; platform: `workshop-platform/kubernetes/base/*`; OS: `workshop-app/k8s/*`; commands captured include `kubectl kustomize`, `kubectl apply --dry-run=server`, `kind create cluster`, `kubectl apply`, `kubectl get pods,svc,deploy,statefulset -A -o wide`, `kubectl exec rabbitmq-0 -- rabbitmq-diagnostics -q ping`, and in-cluster `curl` to `http://workshop-app:3000/health` and `/ready`. | Partial | Replace platform placeholder images with real locally loaded or registry-published OS/Billing/Execution images and rerun a full three-service Kubernetes E2E smoke. Checklist item 3 evidence collection can be marked complete because the live proof and exact blocker are documented. |
| Branch protection | GitHub API read-only evidence now proves protected `stag` and `prod` branches for `workshop-app`, `workshop-edge`, `workshop-platform`, `workshop-db`, `workshop-billing`, and `workshop-execution`, including required status checks and strict mode. Billing and Execution read-back proves required check `ci` on both target branches. | `workshop-app/docs/fase-4/ci-branch-protection-evidence.md`; branch protection APIs: app requires `lint,test,build,container,k8s` on `stag` and adds `promotion-source,drift-report` on `prod`; edge requires `terraform,lambdas` plus promotion checks on `prod`; platform requires `terraform,kubernetes` plus promotion checks on `prod`; db requires `terraform` plus promotion checks on `prod`; Billing/Execution require `ci` on `stag` and `prod` with strict mode true. | Complete | No remaining branch-protection evidence gap for checklist item 4. |
| Swagger/Postman | OpenAPI files are present for OS, Billing, and Execution. Evidence says Billing and Execution docs were updated. No Postman collection was found/required in the inspected evidence. | `workshop-app/docs/openapi.yaml`; `workshop-billing/docs/openapi.yaml`; `workshop-billing/docs/evidence/fase-4/f4-billing-payment-contracts.md`; `workshop-execution/openapi/openapi.yaml`; `workshop-execution/docs/evidence/fase-4/f4-execution-contracts.md`. | Complete | Include OpenAPI links in PDF. Optional: export/import into Swagger UI or Postman for video. |

## Code-boundary evidence vs operational evidence

### Code-boundary evidence already present

- OpenSpec validation and archive records for the Phase 4 changes in app, billing, execution, edge, platform, and db evidence files.
- Local lint/build/test results for each service repository.
- OS saga state machine, durable saga schema/repository, idempotency, and compensation intent tests.
- RabbitMQ envelope/parser/adapter/handler tests in OS, Billing, and Execution without requiring a live broker.
- Billing PostgreSQL migrations/repository/readiness contract, mostly tested through mocked query boundaries.
- Execution MongoDB repository/readiness contract, with readiness defaulting off unless explicitly enabled.
- Kubernetes YAML validation for platform RabbitMQ/service manifests and OS k8s workflow render checks.
- OpenAPI files for the three services.

### Operational evidence still needed

- Full three-service Kubernetes E2E proof beyond the local RabbitMQ smoke harness and OS-only Kubernetes health/ready smoke.
- Real/local RabbitMQ smoke evidence is now present in `docs/fase-4/rabbitmq-smoke-evidence.md`; keep the harness caveat visible in PDF/video.
- Real PostgreSQL and MongoDB persistence proof during the smoke or demo.
- Kubernetes render/apply and live cluster checks are now captured in `docs/fase-4/kubernetes-evidence.md`; remaining Kubernetes gap is a post-deploy cross-service smoke after replacing placeholder service images.
- Green CI/branch-protection evidence is now present for app, edge, platform, db, Billing, and Execution.
- Final PDF and video artifacts.

## Final operational checklist obligations from plan lines 408-420

| Obligation | Evidence found | Commands/files/links | Status | Gap / next action |
|---|---|---|---|---|
| 1. Consolidar matriz requisito → evidência final | This document consolidates the final matrix in the preferred path. | `workshop-app/docs/fase-4/requirements-evidence.md` | Complete | Keep this file updated as RabbitMQ/Kubernetes/CI/PDF evidence is collected. |
| 2. Rodar/provar smoke com RabbitMQ real/local | RabbitMQ code adapters and platform manifests exist. A real local RabbitMQ broker was started and a full dedicated smoke harness exercised OS, Billing, and Execution messaging code against the broker. The run proved a happy path (`f4-rabbitmq-full-happy-1784465966870`) ending with OS `COMPLETED` and Execution `completed`, and a failure/compensation path (`f4-rabbitmq-full-failure-1784465966870`) ending with OS `COMPENSATED`, Billing canceled state, and Execution `failed`. Caveat: this is a local harness around real messaging handlers/publishers, not a Kubernetes or three-HTTP-service-process E2E. | `workshop-app/docs/fase-4/rabbitmq-smoke-evidence.md`; helper: `workshop-app/docs/fase-4/rabbitmq-full-smoke.ts`; broker container: `workshop-f4-rabbitmq-smoke`; code evidence: `workshop-app/docs/evidence/fase-4/f4-os-rabbitmq-events.md`; `workshop-billing/docs/evidence/fase-4/f4-billing-rabbitmq-compensation.md`; `workshop-execution/docs/evidence/fase-4/f4-execution-rabbitmq-events.md`; `workshop-platform/docs/evidence/fase-4/f4-platform-rabbitmq.md`. | Complete | No remaining gap for checklist item 2. Full service-process/Kubernetes proof remains under checklist item 3 and final demo evidence. |
| 3. Validar Kubernetes demonstrável | Local `kind` evidence now proves platform and OS/app render, server-side dry-run, live apply, `kubectl get pods/services/deployments/statefulsets`, RabbitMQ readiness (`rabbitmq-0` Running and `rabbitmq-diagnostics ping`), OS/app rollout (`workshop-app` Running), and in-cluster `/health` + `/ready` HTTP 200. The exact blocker is documented: shared platform Order/Billing/Execution Deployments still use `example.invalid/...:replace-me` placeholder images and show `ImagePullBackOff`, so this is not a full three-service Kubernetes E2E. | `workshop-app/docs/fase-4/kubernetes-evidence.md`; helper: `workshop-app/docs/fase-4/kubernetes-kind-postgres-helper.yaml`; rendered during run: `/tmp/workshop-platform-base.yaml`, `/tmp/workshop-app-stag.yaml`; manifests: `workshop-platform/kubernetes/base/*`, `workshop-app/k8s/*`. | Complete | For a stronger final demo, replace placeholder service images with real OS/Billing/Execution images and rerun three-service Kubernetes smoke; the checklist item itself can be marked complete because live evidence plus honest blocker are captured. |
| 4. Coletar CI/CD independente e branch protection | Evidence collected for app, edge, platform, and db has green PR Validation runs plus protected `stag`/`prod` branches with required checks. Billing and Execution also now have remote `stag`/`prod` branches, active `PR Validation` workflows, successful `stag` workflow-dispatch runs with job `ci`, and GitHub API read-back proving branch protection with required `ci` checks on `stag` and `prod`. Latest `stag` Deploy runs still failed in app/edge/platform/db, but checklist item 4's required CI/branch-protection evidence is complete. | `workshop-app/docs/fase-4/ci-branch-protection-evidence.md`; green PR Validation links: app `https://github.com/tech-challenges-fiap/workshop-app/actions/runs/28380465166`, edge `https://github.com/tech-challenges-fiap/workshop-edge/actions/runs/28380471671`, platform `https://github.com/tech-challenges-fiap/workshop-platform/actions/runs/28380477901`, db `https://github.com/tech-challenges-fiap/workshop-db/actions/runs/28380485369`; Billing `https://github.com/tech-challenges-fiap/workshop-billing/actions/runs/29713968100` job `ci` success; Execution `https://github.com/tech-challenges-fiap/workshop-execution/actions/runs/29713969857` job `ci` success; Billing/Execution branches/protection: `workshop-billing` SHA `c85ec6606be2fb78e64786f49dc918f5bf4ae186`, `workshop-execution` SHA `ea67645e1b191be341f68c3d059a81d3379122f7`, both requiring `ci` with strict mode true. | Complete | No remaining gap for checklist item 4. If deploy success is requested separately, address or caveat failed latest `stag` deploys. |
| 5. Consolidar docs finais, PDF e roteiro de vídeo | Architecture/events/saga/evidence docs exist in pieces, but no final PDF or video artifact was found in the inspected evidence. | Existing docs/evidence under each repo; this matrix under `workshop-app/docs/fase-4/requirements-evidence.md`. | Partial | Build final PDF and video script/demo recording after RabbitMQ, Kubernetes, and CI/branch evidence are collected. |

## Ordered next actions

1. **RabbitMQ smoke first:** run a real/local RabbitMQ broker and start OS, Billing, and Execution with messaging enabled. Execute happy path and compensation path. Save service logs showing published/consumed events and the same `correlationId` across services.
2. **Kubernetes full-service proof:** checklist item 3 now has local `kind` render/apply/health evidence and an honest blocker. For a stronger final demo, replace the platform placeholder images with real OS/Billing/Execution images, deploy all three service pods to Ready, and run a post-deploy cross-service smoke.
3. **CI/CD and branch protection:** evidence file now proves app/edge/platform/db/Billing/Execution PR validation and branch protection. Checklist item 4 is complete.
4. **Coverage sweep:** run `bun run test:coverage` in `workshop-app` and collect current coverage output for all three services in one final evidence bundle.
5. **PDF/video last:** update final docs and produce the PDF plus video script/recording only after the operational RabbitMQ, Kubernetes, and CI evidence is available.
