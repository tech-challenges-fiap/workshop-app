apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: workshop-app
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: workshop-app
                port:
                  name: http
